{
  "scripts": {
    "dev": "parcel './apps/*/index.html'",
    "prebuild": "rm -rf dist",
    "build": "parcel build './apps/*/index.html' --public-url /parcel_demo/"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
